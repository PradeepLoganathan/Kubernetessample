apiVersion: v1
kind: Service
metadata:
  name: {{ template "kubeapispoc.fullname" . }}
  labels:
    app: {{ template "kubeapispoc.name" . }}
    chart: {{ template "kubeapispoc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "kubeapispoc.name" . }}
    release: {{ .Release.Name }}
